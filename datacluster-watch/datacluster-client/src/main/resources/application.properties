# ─── Client Configuration ─────────────────────────────────────────────────────
server.host=localhost
server.rmi.port=1099

# Intervalle de rafraîchissement UI (ms)
client.refresh.interval.ms=3000

# Fenêtre historique pour le graphique temps réel (minutes)
client.chart.history.minutes=5

# Export
client.export.default.dir=${user.home}/datacluster-exports
